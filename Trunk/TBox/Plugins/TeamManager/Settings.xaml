<UserControl x:Class="TeamManager.Settings"
             xmlns:loc="clr-namespace:TeamManager.Code.Localization"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" 
             xmlns:Units="clr-namespace:WPFControls.Components.Units;assembly=WPFControls"
             xmlns:captioned="clr-namespace:WPFControls.Controls.Captioned;assembly=WPFControls">
    <TabControl>
        <TabItem Header="{loc:Tr Persons}">
            <Units:CheckableListBoxUnit ItemsSource="{Binding Persons}" UnitType="Text" Title="{loc:Tr ConfigurePersons}" />
        </TabItem>
        <TabItem Header="{loc:Tr TargetProcess}">
            <StackPanel>
                <captioned:CaptionedTextBox Value="{Binding UserEmail}" Caption="{loc:Tr Login}" Margin="5"/>
                <captioned:CaptionedPasswordBox Value="{Binding UserPassword}" Caption="{loc:Tr Password}" Margin="5"/>
                <captioned:CaptionedTextBox Value="{Binding ProjectManagerUrl}" Caption="{loc:Tr ProjectManagerUrl}" Margin="5"/>
            </StackPanel>
        </TabItem>
    </TabControl>
</UserControl>
