<UserControl x:Class="ServicesCommander.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Units="clr-namespace:WPFControls.Components.Units;assembly=WPFControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <GroupBox Header="Profile:" DockPanel.Dock="Top">
            <Units:ComboBoxUnit Name="Profile" ValueCaption="{Binding SelectedProfile}" ItemsSource="{Binding Profiles}" UnitType="Text" Title="Profile" />
        </GroupBox>
        <Label Content="Windows services:" Margin="0" Padding="0" HorizontalAlignment="Center" DockPanel.Dock="Top"/>
        <Units:CheckableListBoxUnit Name="Services" DataContext="{Binding ElementName=Profile, Path=SelectedValue}" ItemsSource="{Binding Services}" Title="Services" UnitType="DropDownList" />
    </DockPanel>
</UserControl>
