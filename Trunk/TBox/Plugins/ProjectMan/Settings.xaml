<UserControl x:Class="ProjectMan.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" 
             xmlns:Units="clr-namespace:WPFControls.Components.Units;assembly=WPFControls"
             xmlns:captioned="clr-namespace:WPFControls.Controls.Captioned;assembly=WPFControls">
    <TabControl>
        <TabItem Header="Configure solutions folders">
            <Units:CheckableFileListBoxUnit ItemsSource="{Binding Dirs}" UnitType="FolderPath" Title="Configure projects directories" PathGetterType="Folder"/>
        </TabItem>
        <TabItem Header="Build tools pathes">
            <StackPanel>
                <captioned:CaptionedEditPath Value="{Binding PathToSvn}" Caption="Path to svn:" PathGetterType="File" Margin="5"/>
                <captioned:CaptionedEditPath Value="{Binding PathToMsBuild}" Caption="Path to MSBuild:" PathGetterType="File" Margin="5"/>
                <captioned:CaptionedTextBox Value="{Binding SvnUserName}" Caption="UserName:" Margin="5"/>
            </StackPanel>
        </TabItem>
    </TabControl>
</UserControl>
