<components:ExtListBox x:Class="WPFControls.Components.ButtonsView.GroupedList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:buttonsView="clr-namespace:WPFControls.Components.ButtonsView"
                       xmlns:components="clr-namespace:WPFControls.Components"
                       mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" ScrollViewer.CanContentScroll="False" ItemsSource="{Binding Items}" AlternationCount="1">
    <ItemsControl.ItemTemplate>
        <DataTemplate DataType="buttonsView:IButtonInfo">
            <Button Width="64" Height="64" Click="ClickItem" VerticalAlignment="Top" VerticalContentAlignment="Top" BorderThickness="0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" KeyUp="KeyUpItem" Margin="0" Padding="0">
                <StackPanel >
                    <Image Source="{Binding Icon}" HorizontalAlignment="Center" Width="24" Height="24" />
                    <TextBlock Text="{Binding Name}" TextWrapping="WrapWithOverflow" TextTrimming="CharacterEllipsis" TextAlignment="Center"/>
                </StackPanel>
            </Button>
        </DataTemplate>
    </ItemsControl.ItemTemplate>
    <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
            <WrapPanel  Margin="5"/>
        </ItemsPanelTemplate>
    </ItemsControl.ItemsPanel>
    <ItemsControl.GroupStyle>
        <GroupStyle AlternationCount="2">
            <GroupStyle.HeaderTemplate>
                <DataTemplate DataType="GroupItem">
                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                </DataTemplate>
            </GroupStyle.HeaderTemplate>
        </GroupStyle>
    </ItemsControl.GroupStyle>
</components:ExtListBox>
