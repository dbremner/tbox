<UserControl x:Class="Mnk.Library.WpfControls.Components.CheckableTreeView.CheckableTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:loc="clr-namespace:Mnk.Library.WpfControls.Localization"
             xmlns:checkableTreeView="clr-namespace:Mnk.Library.WpfControls.Components.CheckableTreeView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" >
    <DockPanel>
        <StatusBar  HorizontalAlignment="Stretch"  Margin="0,0,6,0" DockPanel.Dock="Bottom">
            <Button Content="{loc:Tr CheckAll}" Click="ButtonCheckAllClick" />
            <Button Content="{loc:Tr UncheckAll}"  Click="ButtonUncheckAllClick" />
            <Button Content="{loc:Tr ExpandAll}" Click="ButtonExpandClick" />
            <Button Content="{loc:Tr CollapseAll}"  Click="ButtonCollapseClick" />
        </StatusBar>
        <TreeView Name="treeView" ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
    </DockPanel>
</UserControl>
